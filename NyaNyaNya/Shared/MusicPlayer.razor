@if(PlayMusic)
{
    <audio autoplay loop class="d-none">
        <source src="music/@(Track).mp3" type="audio/mp3">
    </audio>
}

@code {
    [Parameter]
    public string Track { get; set; }

    [Parameter]
    public bool PlayMusic { get; set; }
}
